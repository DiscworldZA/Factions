package disc.mods.factions.utils;

public class BuildingHelper
{
    /*
     * public static void addFloor(Building building, int y, Block block) { for
     * (int x = 0; x < building.getXSize(); x++) { for (int z = 0; z <
     * building.getZSize(); z++) { building.getBuildingBlocks()[x][y][z] =
     * block; } } }
     * 
     * public static void addWall(Building building, int y, Block block) { for
     * (int x = 0; x < building.getXSize(); x++) { for (int z = 0; z <
     * building.getZSize(); z++) { if ((x == 0 || x == building.getXSize() - 1)
     * || (z == 0 || z == building.getZSize() - 1)) {
     * building.getBuildingBlocks()[x][y][z] = block; } } } }
     * 
     * public static void fillWithAir(Building building) { for (int x = 0; x <
     * building.getXSize(); x++) { for (int z = 0; z < building.getZSize(); z++)
     * { for (int y = 0; y < building.getYSize(); y++) {
     * if(building.building[x][y][z] == null) { building.building[x][y][z] =
     * Blocks.AIR; } } } } }
     */

}
